export interface Formula {
  description: boolean;
  id: string;
  ingredients: Array<any>;
  mixing: Array<any>;
  items: Array<any>;
  name: string;
  steps: Array<any>;
  unit_weight: string;
  units: string;
  user: any;
}
